/**
 * Aragon Media Portal — Drizzle Schema
 *
 * This is a placeholder. The full schema (18 tables) from data-model-spec.md
 * v0.3 will be implemented in the next session:
 *   - users, verification_codes
 *   - tiktok_accounts, tiktok_oauth_tokens, gmv_snapshots
 *   - pricing_rules, orders, order_slots, order_credentials, order_screenshots
 *   - chat_threads, chat_messages, chat_attachments, chat_mentions
 *   - contracts, signed_contracts
 *   - withdrawals
 *   - notifications, files, audit_log
 *
 * Once the Oracle Cloud VM is up and DATABASE_URL is populated, run:
 *   npm run db:generate   # generate migration SQL from this schema
 *   npm run db:migrate    # apply migrations to the DB
 *   npm run db:seed       # seed admin user + pricing_rules + contract v1.0
 */

export const placeholder = "schema-coming-next-session";
